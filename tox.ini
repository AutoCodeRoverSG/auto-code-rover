[tox]
envlist = py312
skipsdist = True
 
[testenv]
skip_install = True
passenv = PYTHONPATH
commands =
    pytest --cov=app test/ --cov-report=term --cov-report=xml --cov-config=tox.ini --cov-branch
