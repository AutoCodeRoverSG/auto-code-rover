[tox]
envlist = py312
skipsdist = True
 
[testenv]
passenv = PYTHONPATH
deps =
    pytest
    coverage
commands =
    coverage run -m pytest
    coverage xml
 
[coverage:run]
relative_files = True
source = app/
branch = True
